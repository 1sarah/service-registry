eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
server:
  port: 8761
  wait-time-in-ms-when-sync-empty: 0

management:
  endpoints:
    web:
      exposure:
        include: [ "prometheus" ]